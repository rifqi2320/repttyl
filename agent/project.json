{
  "name": "agent",
  "root": "agent",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go build -o bin/repttyl ./cmd/repttyl",
        "cwd": "agent"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go test ./...",
        "cwd": "agent"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "test -z \"$(gofmt -l ./cmd ./internal)\" && go test -count=1 ./...",
        "cwd": "agent"
      }
    },
    "fmt": {
      "executor": "nx:run-commands",
      "options": {
        "command": "gofmt -w ./cmd ./internal",
        "cwd": "agent"
      }
    }
  }
}
